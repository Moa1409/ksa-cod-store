ENV=production

# Database — paste EasyPanel's connection string as-is (postgres:// works; auto-converted).
# EasyPanel internal example:
# postgres://lamsaglow:PASSWORD@lamsaglow_database:5432/lamsaglow?sslmode=disable
DATABASE_URL=postgres://lamsaglow:CHANGE_ME@lamsaglow_database:5432/lamsaglow?sslmode=disable
# CORS (comma separated)
CORS_ORIGINS=https://lamsaglow.shop,https://www.lamsaglow.shop

    # Admin — dashboard login + API key
ADMIN_TOKEN=CHANGE_ME_LONG_RANDOM
ADMIN_USERNAME=admin
ADMIN_PASSWORD=CHANGE_ME_STRONG_PASSWORD
ADMIN_SESSION_SECRET=CHANGE_ME_LONG_RANDOM_SESSION_SECRET

# Google Sheet webhook — paste your Apps Script Web App /exec URL after deploy.
# Setup: docs/assets/google-apps-script.gs → Deploy → Web app → Anyone → copy URL.
# Example: https://script.google.com/macros/s/AKfycbx.../exec
GOOGLE_SHEET_WEBHOOK_URL=
SHEET_SHARED_SECRET=CHANGE_ME_MATCHES_APPS_SCRIPT

# CAPI master switch
ENABLE_CAPI=true

# Meta Conversions API
META_PIXEL_ID=
META_CAPI_TOKEN=
META_TEST_EVENT_CODE=
META_API_VERSION=v21.0

# TikTok Events API
TIKTOK_PIXEL_ID=
TIKTOK_CAPI_TOKEN=
TIKTOK_TEST_EVENT_CODE=

# Snapchat Conversions API
SNAP_PIXEL_ID=
SNAP_CAPI_TOKEN=

# MaxMind GeoIP2 Insights — block non-KSA / VPN / proxy orders
# Create credentials at https://www.maxmind.com/en/accounts/current/license-key
MAXMIND_ORDER_CHECK_ENABLED=true
MAXMIND_ACCOUNT_ID=
MAXMIND_LICENSE_KEY=
MAXMIND_REQUIRE_KSA=true
MAXMIND_BLOCK_VPN_PROXY=true

# Phones that bypass geo check (comma-separated, for prod testing)
ORDER_WHITELIST_PHONES=0550000000

# Test numbers exempt from fake-number rejection (comma-separated)
TEST_ORDER_PHONES=0550000000
